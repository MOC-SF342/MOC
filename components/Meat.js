import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Meat = ({ navigation, route }) => {
  return (
    <View>
      <Text>Meat</Text>
    </View>
  )
}

export default Meat

const styles = StyleSheet.create({})